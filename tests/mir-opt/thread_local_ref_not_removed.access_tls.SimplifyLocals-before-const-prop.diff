- // MIR for `access_tls` before SimplifyLocals-before-const-prop
+ // MIR for `access_tls` after SimplifyLocals-before-const-prop
  
  fn access_tls() -> () {
      let mut _0: ();
-     let _1: u32;
      let mut _2: *mut u32;
  
      bb0: {
-         StorageLive(_1);
          StorageLive(_2);
          _2 = &/*tls*/ mut TLS_VAR;
-         _1 = copy (*_2);
          StorageDead(_2);
-         StorageDead(_1);
          _0 = const ();
          return;
      }
  }
  
